To say hello using PowerShell, you can simply use the `Write-Host` cmdlet. Here's a command to print "Hello, World!" to the console:

```powershell
Write-Host "Hello, World!"
```

You can run this in your PowerShell terminal, and it will display the message.
